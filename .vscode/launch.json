{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compilar C 64 bits",
      "type": "shell",
      "command": "C:/msys64/mingw64/bin/gcc.exe",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.exe"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Compila o programa usando gcc 64 bits"
    }
  ]
}
